<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

---

# 设计规范

## 全局规范

> **适用范围：全站所有路由与组件。** 本节定义 STREET POP 的基因；「首页」「子页面」两节只声明各自的分工、尺度与加减法，凡本节已写明的规则不再重复。

STREET POP · 街头波普设计语言。气质锚点：街头的、漫画的、波普的——高对比、硬边缘、能量感，"有点吵闹但绝对诚实"。同一套基因，两种音量：首页是海报，子页面是装帧精良的杂志内页。

### 1. 最高原则：图形即内容

每个图形必须能回答"它在传达什么"。形状是 UX 的组成部分——它要么承载信息（曲线、进度条、计数），要么完成叙事（分隔、强调、拟声），要么引导行为（光标、悬停反馈）。回答不了这个问题的图形一律删除。宁可留白，不要摆一个"不明所以"的几何图形当壁纸。纯装饰是最低级的用法，能删就删。

### 2. 配色

- **双色基底**：纸白 + 墨黑。两者都是"近色"而非纯色——纸白带暖灰，墨黑带暖意。
- **高饱和强调色**：最多 3 个，必须高饱和、高亮度——选择标准：在纸白和墨黑两种底色上都要炸得开（可自行用对比度验证），三色之间色相尽量拉开、互不近似。
- **禁令**：不要渐变（特别是蓝紫渐变）、不要低饱和莫兰迪色、不要玻璃拟态、不要大面积阴影柔化。

### 3. 字体（三字体）

| 角色           | 要求                         | 用法                           |
| -------------- | ---------------------------- | ------------------------------ |
| 展示体（拉丁） | 超粗、浓缩、无衬线（海报体） | 巨型标题、空心描边字、横幅大字 |
| 中文黑体       | 最粗字重（900）              | 中文标题、强调短句             |
| 等宽体         | 几何感 mono                  | 编号、标签、元信息、小字注释   |

- 中文**禁用斜体**（机械伪斜体会毁掉版面）。强调用底色高亮、加粗、变色。
- 中西文混排时字体栈必须同时包含拉丁展示体和中文字体：`'DisplayLatin', 'ChineseHei', sans-serif`。
- 层级靠**尺寸悬殊**拉开；悬殊幅度按页面类型而定（见「首页」「子页面」两节）。
- 空心描边字（`-webkit-text-stroke` + 透明填充）是标志性手法；允许出现的位置按页面类型而定。

### 4. 形状

- **一律直角**：按钮、卡片、标签零圆角；边框 2–4px 实线。
- **硬阴影**：`box-shadow` 纯偏移、无模糊（如 `6px 6px 0 墨色`，具体尺度按页面类型而定）；悬停时阴影缩短、元素位移，模拟"按下去"。
- **方块（square block）**：实心正方形色块，粗描边 + 硬阴影。块面上必须承载文字或数字（如 KEEP TRAINING、EP.07），不放空色块当装饰。
- **虚线选框**：选中态用虚线框 + 角上小方块手柄。

### 5. 材质

- 全页覆盖一层 **动态噪点**（SVG feTurbulence data-URI，opacity 0.05，steps 跳帧动画），模拟印刷颗粒。这是全站材质签名，不干扰阅读。
- 除此之外表面一律平坦。不要模糊、不要渐变、不要高光。

### 6. 动效底律

- 统一缓动签名：**expo-out**——`cubic-bezier(.19, 1, .22, 1)`，快入缓出，果断不黏。
- **悬停擦除**：反色填充用 `clip-path: inset(0 100% 0 0) → inset(0 0 0 0)` 从左擦入，而不是变色。
- **尊重 `prefers-reduced-motion`**：降级为全部静态可见。

### 7. 反模式（任何页面出现即违规）

- 圆角卡片 + 柔和投影 + 渐变背景
- 居中 hero + 两个 CTA 按钮 + 三栏特性卡片的模板布局
- emoji 图标、图标字体方块
- 蓝紫渐变、霓虹 glow、毛玻璃
- 淡入淡出作为唯一的动效
- 大段正文无层级地平铺
- 与内容和功能无关的几何图形、网点、色块充当背景壁纸
- 装饰性视差（不承载信息的背景元素随滚动乱飘）

## 首页

> **适用范围：仅首页 `app/(home)/home`。** 海报模式：把全局基因推到极限——能量优先，宁可吵，不可温吞。本节是该页面视觉与动效实现的唯一依据（在全局规范之上）。

### 1. 气质强化

- **能量优先**：页面应该像一张贴在墙上的海报，有态度、有音量。
- **设计即 UX**：风格元素必须长在交互与信息里。曲线、进度条、滚动指示、悬停反馈——用户看到的每个"设计"，最好同时是一件能用的东西。
- **诚实的手感**：不假装精致。硬阴影、粗描边、错位拼贴——像手工剪贴，不像软件渲染。
- **动效有性格**：元素入场要"蹦"出来，不是"淡"出来。过场要有戏剧性。

### 2. 配色用法

- 强调色只用于：标题高亮、贴纸、横幅、关键数字、悬停态。面积小、爆发力强。
- **明暗交替**：页面在长滚动中交替使用纸底与黑底区块，形成节拍感。

### 3. 字体用法

- 标题可以是正文的 8–15 倍。
- 空心描边字用于副标题、背景大字、页脚。

### 4. 图形库（全局形状之上，首页额外允许）

1. **网点圆（halftone）**：`radial-gradient` 点阵，漫画印刷质感。**限叙事用途**：只出现在与"印刷/屏幕质感"内容直接相关的位置（如作品面板底图），不做整屏背景。
2. **棋盘格（checker）**：`repeating-conic-gradient` 黑白格。赛车旗/像素的双重联想，小面积使用，仅用于分隔带。
3. **方块错位叠放**：两块错位叠放（一块实心、一块仅描边轮廓）。
4. **全宽条带**：一律水平，全宽贯通，不旋转。
5. **旋转**：小型元素（贴纸、卡片、标签）可以旋转 ±1.5°~3° 制造手工感，但同一屏内旋转角度不超过两种；全宽条带不在此列。

硬阴影尺度：`6px 6px 0 墨色`。

### 5. 构图与布局

- **非对称 12 列网格**：内容在左右之间锯齿交替（zigzag），禁止从头到尾居中堆叠。
- **重叠与出血**：图形可以压字、出血出屏、互相叠压。用 `overflow:hidden` 控制出血边界。
- **编号系统**：每个区块带等宽体编号（01 / 02…），编号本身可以做成旋转的小标签或巨大的空心数字。
- **竖排文字**：中文竖排（`writing-mode: vertical-rl`）作为边角的态度签名。
- **呼吸感**：区块之间留大空白（12vh+ 内边距），让高能量元素有处安放。

### 6. 动效清单（全局底律之上，以下手法全部允许）

- **入场编排（choreographed sequence）**：开场必须是一条时间线：加载计数 → 遮罩升起 → 主标题逐字弹入 → 贴纸旋转弹出 → 元信息淡入。多步骤、有门控，不是一个 fade 糊弄。
- **逐字/逐词**：大标题按字符交错入场（`yPercent: 110 → 0`，stagger 0.03–0.06）；长句宣言随滚动**逐词点亮**（从 12% 透明度到 100%，scrub 绑定滚动）。
- **线条绘制**：SVG 线段/曲线用 `pathLength: 1` + `stroke-dashoffset` 在滚动到位时画出来。
- **跑马灯**：无限横向滚动条带，相邻两条方向相反、速度不同。内容无缝循环（DOM 复制一份）。
- **视差**：只允许"内容级"视差——如作品画框整体 ±40px 反向漂移，强化阅读节奏；不给无信息量的背景图形做视差。
- **数字计数**：统计数字滚动到视口内时从 0 跳到目标值（padStart 补零保持等宽）。

## 子页面

> **适用范围：首页以外的所有路由**（`/articles`、`/projects`、`/contact`、`/music` 等，含今后新增的子页面）。定位：装帧精良的杂志内页——内容优先，风格是装帧；任何与阅读竞争注意力的元素都算违规。

### 1. 统一外壳（所有子页面共享同一布局）

- 所有子页面必须共用**同一个外层布局组件**（页眉 + 页脚），禁止各页自建框架：
  - **页眉**：站点标识（点击回首页）+ 主导航，全站统一。
  - **页面标题区**：统一格式——mono 小标签（类别/编号）+ 黑体大标题 + 一行元信息（日期、计数等）。
  - **内容区**：限宽居中（`max-w-3xl`～`4xl`）；长文正文进一步收窄到阅读宽度。
  - **页脚**：站点标识 + 一行联系/导航信息。
- 页面之间只允许内容区不同；外壳、标题区结构、间距尺度必须一致。

### 2. 配色

- **纸底阅读**：正文与列表一律纸白底；墨黑用于页眉、页脚与小面积强调区块。
- 强调色每屏最多出现一处（一个高亮、一个标签、一个悬停态）。

### 3. 字体与可读性

- 尺寸悬殊以读感为先：页面标题为正文的 2.5–4 倍。
- 正文 15–16px、行高 ≥ 1.9、行长 30–40em。
- 长文排版基于 `@tailwindcss/typography`（prose），只覆盖颜色、边框与标题字重，不重写整套样式。
- 空心描边字用于页脚或标题区的装饰编号。

### 4. 形状

- 图形一律承担内容职能：编号方块、标签、虚线选中框。
- 硬阴影取 `4px 4px 0`。
- 区块分隔用 2–3px 水平实线或留白。

### 5. 动效

- 首屏直接呈现内容，动效服务于阅读节奏，一次入场即结束。
- 区块入场 reveal：上浮 + autoAlpha，once。
- 悬停反馈按全局底律：clip-path 擦除、阴影缩短 + 位移、箭头平移。
- 页内锚点平滑滚动。
