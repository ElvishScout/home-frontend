// Placeholder module for `virtual:article-registry`.
//
// `next.config.ts` aliases the `virtual:article-registry` import to this file
// and attaches `loader.js` to it via `turbopack.rules`. The loader ignores this
// content and emits the article registry in its place — this file exists only
// because Turbopack rules can only run on files that actually resolve.
export default {};
